# The Connect News

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```

Photo card and Facebook publishing: see `docs/social-publishing.md`.
Required server secrets: `META_ACCESS_TOKEN`, `META_PAGE_ID`. Optional: `SITE_URL`.
